# پروژه رزرو پرواز - Borj Sefid

یک پلتفرم کامل برای رزرو پرواز آنلاین با Django

## 📋 ساختار اپ‌ها

### 1. **Flights** - مدیریت پروازها
- لیست تمام پروازهای فعال
- جستجو بر اساس شهر مبدأ و مقصد
- مشاهده جزئیات پرواز

**مدل‌ها:**
- `Flight`: شامل اطلاعات پرواز، قیمت، صندلی‌ها

### 2. **Passengers** - مدیریت مسافران
- ذخیره اطلاعات مسافران
- کد ملی و شماره پاسپورت

**مدل‌ها:**
- `Passenger`: اطلاعات شخصی مسافر

### 3. **Bookings** - مدیریت رزروها
- ایجاد رزرو جدید
- مشاهده تاریخچه رزروها
- تخصیص صندلی‌ها

**مدل‌ها:**
- `Booking`: اطلاعات رزرو
- `BookingPassenger`: ارتباط مسافران با رزرو

### 4. **Payments** - مدیریت پرداخت‌ها
- پردازش پرداخت
- ردگیری وضعیت پرداخت
- روش‌های مختلف پرداخت (کارت، انتقال بانکی)

**مدل‌ها:**
- `Payment`: اطلاعات پرداخت

### 5. **Accounts** - مدیریت کاربران
- پروفایل کاربر
- تصویر پروفایل
- تایید هویت

**مدل‌ها:**
- `UserProfile`: پروفایل کاربر

---

## 🚀 شروع کار

### نصب وابستگی‌ها
```bash
pip install -r requirements.txt
```

### مایگریشن‌ها
```bash
python manage.py migrate
```

### ایجاد ادمین
```bash
python manage.py createsuperuser
```

### اجرای سرور
```bash
python manage.py runserver
```

---

## 📍 URLs

| URL | توضیح |
|-----|--------|
| `/admin/` | پنل مدیریت |
| `/flights/` | لیست پروازها |
| `/flights/<id>/` | جزئیات پرواز |
| `/bookings/` | لیست رزروهای کاربر |
| `/bookings/<id>/` | جزئیات رزرو |
| `/bookings/create/<flight_id>/` | ایجاد رزرو |

---

## 📊 نمودار پایگاه داده

```
Flight (پرواز)
├── flight_number
├── airline
├── origin_city
├── destination_city
├── departure_time
├── arrival_time
├── aircraft_type
├── total_seats
├── available_seats
└── price_per_seat

Passenger (مسافر)
├── user (FK: User)
├── first_name
├── last_name
├── national_id
├── passport_number
└── date_of_birth

Booking (رزرو)
├── booking_code
├── user (FK: User)
├── flight (FK: Flight)
├── booking_date
├── status
└── total_price

BookingPassenger (مسافر رزرو)
├── booking (FK: Booking)
├── passenger (FK: Passenger)
└── seat_number

Payment (پرداخت)
├── booking (FK: Booking)
├── amount
├── payment_method
├── payment_date
└── status

UserProfile (پروفایل کاربر)
├── user (FK: User)
├── phone_number
├── national_id
├── address
├── city
└── is_verified
```

---

## 🔐 کنترل دسترسی

- **مسافران** می‌توانند فقط رزروهای خود را مشاهده کنند
- **ادمین** دسترسی کامل به تمام داده‌ها دارد

---

## 📝 نکات مهم

- تمام تاریخ‌ها و ساعت‌ها با منطقه‌زمانی تهران است
- قیمت‌ها به تومان محاسبه می‌شوند
- صندلی‌ها به صورت خودکار هنگام رزرو کم می‌شوند

---

## 👨‍💻 توسعه‌دهندگان

ساخته شده با ❤️ با Django
